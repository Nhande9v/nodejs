<h2>Nhập chỉ số điện cho phòng {{phongtro.tenphong}} ({{phongtro.idphong}})</h2>
<form method="POST">
  <input type="hidden" name="idphong" value="{{phongtro.idphong}}">
  <div class="form-group">
    <label>Tháng:</label>
    <input name="thang" class="form-control" required placeholder="07/2025" value="{{thang}}">
  </div>
  <div class="form-group">
    <label>Chỉ số cũ:</label>
    <input name="chisocu" type="number" class="form-control" required value="{{dien.chisocu}}">
  </div>
  <div class="form-group">
    <label>Chỉ số mới:</label>
    <input name="chisomoi" type="number" class="form-control" required value="{{dien.chisomoi}}">
  </div>
  <div class="form-group">
    <label>Đơn giá:</label>
    <input name="dongia" type="number" class="form-control" value="3500" required value="{{dien.dongia}}">
  </div>
  <button type="submit" class="btn btn-success mt-3">Lưu chỉ số điện</button>
</form>