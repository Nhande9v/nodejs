<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">
    Nhà trọ tình thương
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><a class="nav-link" href="/">Trang chủ</a></li>
      <li class="nav-item"><a class="nav-link" href="/news">Tin tức</a></li>
      {{#if user}}
        {{#if (eq user.role 'admin')}}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="danhSachDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Danh sách
            </a>
            <div class="dropdown-menu" aria-labelledby="danhSachDropdown">
              <a class="dropdown-item" href="/khachthue">Khách thuê</a>
              <a class="dropdown-item" href="/hopdong">Hợp đồng</a>
            </div>
          </li>
        {{/if}}
      {{/if}}
    </ul>
    <div class="ml-auto">
      {{#if user}}
        <span class="mr-3">
          <i class="fa fa-user"></i> Xin chào, {{user.ten}}
          {{#if (eq user.role 'admin')}} <span class="badge badge-warning">Chủ trọ</span>{{/if}}
        </span>
        <a href="/logout" class="btn btn-secondary">Đăng xuất</a>
      {{else}}
        <a href="/login" class="btn btn-primary mr-2">Đăng nhập</a>
        <a href="/dang-ky" class="btn btn-success">Đăng ký</a>
      {{/if}}
      {{#if isUser}}
        <a href="/hopdong/my" class="btn btn-info ml-2">Hợp đồng của tôi</a>
      {{/if}}
    </div>
  </div>
</nav>